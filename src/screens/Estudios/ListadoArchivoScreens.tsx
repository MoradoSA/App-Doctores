import { Layout, Text } from '@ui-kitten/components'
import React from 'react'
import { ScrollView } from 'react-native-gesture-handler'

export const ListadoArchivoScreens = () => {
  return (
    <Layout style={{
        flex: 1,
        backgroundColor: '#fff'
    }}>
        <ScrollView>

        </ScrollView>
    </Layout>
  )
}


